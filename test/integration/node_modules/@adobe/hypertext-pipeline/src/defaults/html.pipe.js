// default processing pipeline for HTML
const pipeline = require('../../index');

module.exports.pipe = function(cont, params) {
    console.log('Constructing HTML Pipeline');
    return pipeline()
        .once(cont);
}